package com.techentrance.techentrance.MainGET;

import com.techentrance.techentrance.Utils;
import com.techentrance.techentrance.controller.LoginController;
import com.techentrance.techentrance.model.User;
import com.techentrance.techentrance.security.Security;
import com.techentrance.techentrance.service.UserService;
import org.junit.jupiter.api.BeforeEach;
import org.junit.jupiter.api.Test;
import org.junit.runner.RunWith;
import org.mockito.Matchers;
import org.mockito.Mock;
import org.mockito.Mockito;
import org.mockito.MockitoAnnotations;
import org.springframework.ui.Model;

import static org.junit.Assert.*;

import javax.servlet.http.Cookie;
import javax.servlet.http.HttpServletRequest;
import javax.servlet.http.HttpServletResponse;

import java.util.UUID;

import static org.mockito.ArgumentMatchers.any;
import static org.mockito.Mockito.when;


public class SecurityTest {

    @Mock
    private UserService userServiceMock;

    @Mock
    private HttpServletResponse httpServletResponseMock;

    @Mock
    private Utils utilsMock;

    @Mock
    private HttpServletRequest httpServletRequestMock;

    @Mock
    private User userMock;

    @Mock
    private Model modelMock;
    
    private List<APIObject> arrayListMock;

    @BeforeEach
    public void before() {
        MockitoAnnotations.initMocks(this);
        //mainGET = new MainGET(arrayListMock);
    }

    @Test
    public void exception_generated() {
        when(MainGET.ArrayList<>()).thenReturn(null);
        Object response = MainGET.getList();

        assertNull(response);
    }

    @Test
    public void exception_not_generated() {
        //when(mainGETMock.ArrayList<>()).thenReturn(null);
        Object response = MainGET.getList();

        assertNotNull(response);
    }

   
}
